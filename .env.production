# 生产环境配置（服务器部署）
# 这个地址会在构建时注入到代码中

# 方案1：使用服务器域名+端口（如果后端在8000端口）
VITE_API_BASE_URL=http://aippof-0w0.lthero.com:8000

# 方案2：如果您配置了Nginx反向代理（推荐）
# VITE_API_BASE_URL=https://aippof-0w0.lthero.com/api

# 方案3：如果后端和前端在同一服务器，可以使用相对路径
# VITE_API_BASE_URL=/api
